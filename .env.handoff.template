# Relay — Backend secrets for Girik
# Created: 2026-04-19 ~02:15 AM
# Once filled, share via 1Password shared vault or hat.sh (NOT in chat plaintext).
# After sharing, DELETE this file or gitignore it. Never commit filled secrets.

# === ELEVENLABS ===
# API key: Dashboard -> Profile + API Key
ELEVENLABS_API_KEY=sk_PASTE_FROM_DASHBOARD

# Agent IDs: open each agent, copy from URL (after /agents/)
ELEVENLABS_DETENTION_AGENT_ID=PASTE_FROM_URL
ELEVENLABS_BROKER_UPDATE_AGENT_ID=PASTE_FROM_URL
ELEVENLABS_DRIVER_AGENT_ID=PASTE_FROM_URL

# Phone number IDs — ONE PER AGENT (three numbers total)
# Dashboard -> Phone Numbers -> click each -> copy phnum_... from URL
# Each is bound to its agent for inbound; same number is used for that agent's outbound calls.
ELEVENLABS_DETENTION_PHONE_NUMBER_ID=phnum_PASTE_FROM_URL
ELEVENLABS_BROKER_UPDATE_PHONE_NUMBER_ID=phnum_PASTE_FROM_URL
ELEVENLABS_DRIVER_PHONE_NUMBER_ID=phnum_PASTE_FROM_URL

# Actual phone numbers (for reference / display)
TWILIO_DETENTION_NUMBER=+16204004674
TWILIO_BROKER_UPDATE_NUMBER=+1PASTE_NEW_NUMBER
TWILIO_DRIVER_NUMBER=+1PASTE_NEW_NUMBER

# Post-call webhook signing secret: Settings -> Webhooks -> your webhook -> copy shared secret
ELEVENLABS_WEBHOOK_SECRET=whsec_PASTE_FROM_DASHBOARD

# === TWILIO ===
# console.twilio.com -> Account Info panel
TWILIO_ACCOUNT_SID=AC_PASTE_FROM_CONSOLE
TWILIO_AUTH_TOKEN=PASTE_FROM_CONSOLE
# (Individual phone numbers listed above under ElevenLabs section)

# === ANTHROPIC ===
# console.anthropic.com -> API Keys
ANTHROPIC_API_KEY=sk-ant-PASTE_FROM_CONSOLE

# === BACKEND URL ===
BACKEND_URL=https://relay-truckerpath-b1b6f88e3d10.herokuapp.com

# Shared bearer token for tool calls ElevenLabs -> Heroku backend
# Stored as secret__relay_token dynamic variable on each ElevenLabs agent
RELAY_INTERNAL_TOKEN=99484f20-f8bb-4941-8e29-79fa3c04efda

# === INFRASTRUCTURE (Girik generates himself) ===
# DATABASE_URL - his Postgres host
# PUSHER_APP_ID / KEY / SECRET / CLUSTER - his pusher.com account

# === NAVPRO / TRUCKER PATH ===
# Leave as mock for now — flip to navpro once credentials.json arrives
RELAY_ADAPTER=mock
NAVPRO_BASE_URL=https://api.truckerpath.com/navpro
NAVPRO_CREDENTIALS_PATH=./relay-credentials.json
NAVPRO_WEBHOOK_SECRET=
